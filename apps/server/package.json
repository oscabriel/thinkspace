{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "test:workers": "vitest run --config vitest.workers.config.ts"
  },
  "dependencies": {
    "@thinkspace/db": "workspace:*",
    "@thinkspace/domain": "workspace:*",
    "@thinkspace/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.0",
    "@cloudflare/workers-types": "catalog:",
    "@thinkspace/config": "workspace:*",
    "@types/node": "catalog:",
    "alchemy": "catalog:",
    "jose": "^6.1.0",
    "tsdown": "^0.21.9",
    "typescript": "catalog:",
    "vitest": "^4.1.9",
    "wrangler": "catalog:"
  }
}
